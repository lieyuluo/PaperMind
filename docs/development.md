# Development Guide

## Local Setup

1. Copy `.env.example` to `.env`.
2. Install dependencies after the technology stack is selected.
3. Run tests before opening a pull request.

## Code Location

Application code belongs in `src/`.

Tests belong in `tests/`.

Project documentation belongs in `docs/`.

Automation scripts belong in `scripts/`.
